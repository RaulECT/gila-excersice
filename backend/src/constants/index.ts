export enum NOTIFICATION_AGENTS {
  SMS = 'SMS',
  EMAIL = 'E-mail',
  PUSH_NOTIFICATION = 'Push Notification'
}

export enum MESSAGE_CATEGORIES {
  SPORTS = 'Sports',
  FINANCE = 'Finance',
  MOVIES = 'Movies'
}

export enum DICTIONARY {
  DEFAULT = 'Default'
}

export enum ERROR_MESSAGES {
  INVALID_MESSAGE_ID = 'Invalid message category ID',
  MESSAGE_CATEGORY_NOT_FOUND = 'Cannot find message category'
}

export enum DEFAULT_CONFIG {
  PORT = 4000
}
